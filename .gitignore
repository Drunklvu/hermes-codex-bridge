# Secrets & credentials — never commit these
*.env
.env.*
!*.example
auth.json
*.token
bridge.token
credentials*
id_rsa*
*.pem

# Runtime state (task records, logs, sessions)
.state/
.codex-a2a/
*.log
sessions.json
tasks.json
mcp_inflight.json
*outbox*.jsonl
*outbox*.ack
inbound_outbox.jsonl
inbound_outbox.ack

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Build outputs
dist/
build/
ModuleAnalysisCache
# Machine-local files generated by the installer
scripts/start_bridge.ps1

# SQLite (a2a sidecar persistence)
*.db
*.sqlite

# Coverage
.coverage
htmlcov/
